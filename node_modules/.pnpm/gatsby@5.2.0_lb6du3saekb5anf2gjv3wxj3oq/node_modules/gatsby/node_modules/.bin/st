#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../../st@2.0.0/node_modules/st/bin/server.js" "$@"
else
  exec node  "$basedir/../../../../../st@2.0.0/node_modules/st/bin/server.js" "$@"
fi
